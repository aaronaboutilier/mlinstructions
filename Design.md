Here is a detailed breakdown of the wireframe to help you structure your UI tree and apply the specific design tokens. The layout follows a standard vertical column structure with four distinct horizontal sections.

1. Top Navigation / App Bar
Background Color: Light Blue (#CBE4FF)

Layout: A horizontal row with space between the elements.

Left Element: Text reading "MLINSTRUCTIONS" (All caps).

Right Element: A language toggle or text button reading "EN/FR".

2. Hero Section
Background Color: Off-White (#F9FCFF)

Padding: Standard padding around the content.

Layout: Vertical column, left-aligned.

Typography & Content:

Heading: Labeled as "T3" style. Text reads "Hero All Caps / Two Lines Max".

Heading Color: Dark Navy (#001933).

Constraint: The heading must be restricted to a maximum of two lines.

Button:

Label: "CTA"

Background Color: Yellow (#FFC943).

Shape: Pill-shaped (fully rounded corners).

3. Main Content Section
Background Color: Dark Blue (#00346E)

Padding: Standard padding around the content.

Layout: Vertical column containing introductory text followed by a list of cards.

Introductory Text:

Text reads "Paragraph here 3 lines max".

Color: White or a very light color to contrast with the dark background.

Constraint: Restricted to a maximum of three lines.

Card List: Three distinct elevated cards stacked vertically.

Card Background: White (implied by the drawing).

Card 1: Title "Group CPR Classes" + placeholder body text (1-2 lines).

Card 2: Title "Private Instruct" + placeholder body text (1-2 lines).

Card 3: Title "Refresher Course" + placeholder body text (1-2 lines).

4. Bottom Footer / CTA Section
Background Color: Appears to match the top navigation Light Blue (#CBE4FF).

Padding: Standard padding around the content.

Layout: Vertical column, left-aligned.

Typography:

Heading: "READY 2 GET STARTED?" (All caps).

Body: Placeholder text lines below the heading.

Button:

Label: "CTA"

Background Color: Yellow (#FFC943).

Shape: Pill-shaped (matching the top CTA button).